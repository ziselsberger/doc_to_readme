def mean(x: int = 1, y: int = 2) -> int:
    """Calculate mean of x and y.

    :param x: First number
    :param y: Second number
    :return: Mean of first number (x) and second number (y).
    """
    result = (x + y) / 2
    return result
